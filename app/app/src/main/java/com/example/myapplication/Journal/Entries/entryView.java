package com.example.myapplication.Journal.Entries;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

import com.example.myapplication.R;

public class entryView extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.viewentry_layout);

        //so it lowkey looks ok so far
        /**
         * so take the journal entry pass it here and send to this activity
         * and try to cetnr it too babe
         * then for replaceing it
         * dailyyy!!
         */
    }
}